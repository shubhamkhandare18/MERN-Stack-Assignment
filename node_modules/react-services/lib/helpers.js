module.exports.merge = function(a, b) {
  var key, value, x;
  x = {};
  for (key in a) {
    value = a[key];
    x[key] = value;
  }
  for (key in b) {
    value = b[key];
    x[key] = value;
  }
  return x;
};
